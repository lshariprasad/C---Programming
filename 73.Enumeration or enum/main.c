//Enumeration or enum
#include<stdio.h>
enum bool{No=20,Yes=40};
int main()
{
    enum point {x=18,y};
    enum bool a;
    enum bool b;
    a=Yes;
    b=y;
    printf("\nA value : %d",a);
    printf("\nB value : %d",b);
    return 0;
}
